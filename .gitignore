# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib
# Build artifacts
*.o
*.a

# Test binary, generated by `go test -c`
*.test
# Test results, generated by `go test -v -json`
*.out

# Dependency caches
# Go module cache (if not using vendoring)
# This is usually located in the user's home directory, but can be overridden
# by GOPATH or GOCACHE environment variables.
# If you are vendoring, you might want to include the 'vendor/' directory
# in your repository, but if not, it should be ignored.
vendor/

# Editor-specific files
.vscode/
.idea/
*.swp
*.bak
*~

# Operating System files
.DS_Store
Thumbs.db

# Environment variables (if used)
.env
deploy.yaml

# example-backend-service. express
node_modules