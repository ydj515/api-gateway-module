# Ignored local setup files copied into Codex-managed worktrees.
# Only matching Git-ignored files are copied; file contents stay untracked.

# Local Go gateway and example backend environment.
/.env
/example-backend-service/.env
# Machine-specific deployment configuration already excluded by Git.
/deploy.yaml
